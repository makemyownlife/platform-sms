#########################################本地开发环境#########################################
spring:
  application:
  name: "platform-sms-worker"
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  profiles:
    active: test

server:
  port: 8082
  servlet:
    context-path: /
    encoding:
      enabled: true
      charset: "UTF-8"
      force: true

logging:
  config: "classpath:logback-spring.xml"


